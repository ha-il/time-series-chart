import React from 'react';

function XAxis({ className }: { className: string }) {
  return (
    <div className={className} style={{ backgroundColor: 'tomato' }}>
      x축
    </div>
  );
}
export default XAxis;
